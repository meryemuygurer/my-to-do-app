const task = document.querySelector('todo-input').value;
const taskArea = document.getElementById('task-ul');

function taskAdd(task){
    const task = task;
    const li = document.createElement('li');
    
}